<webscript>
  <shortname>Retrieve all versions (getAllVersions)</shortname>
  <description>
<![CDATA[
Returns the list of all document versions for the specified version series, sorted by CREATION_DATE descending.<br>
<br>
Inputs:<br>
<br>
ID versionSeriesId: Version series Id<br>
<br>
Outputs:<br>
<br>
(Optional) String filter: Property Filter<br>
Collection documentCollection - Set of objects containing Ids and specified properties<br>
<br>
Notes:<br>
<br>
Returns all versions the user can access including checked-out version and private working copy.<br>
]]>
  </description>
  
  <!-- by object id -->
  <url>/cmis/i/{id}/versions?filter={filter?}</url>
  <url>/cmis/s/{store}/i/{id}/versions?filter={filter?}</url>
  <!-- by path -->
  <url>/cmis/p{path}/versions?filter={filter?}</url>
  <url>/cmis/s/{store}/p{path}/versions?filter={filter?}</url>
  <!-- alfresco style -->
  <url>/api/node/{store_type}/{store_id}/{id}/versions?filter={filter?}</url>
  <url>/api/path/{store_type}/{store_id}/{path}/versions?filter={filter?}</url>
  
  <authentication>user</authentication>
  <transaction allow="readonly"/>
  <format default="atomfeed">argument</format>
  <family>CMIS</family>
</webscript>