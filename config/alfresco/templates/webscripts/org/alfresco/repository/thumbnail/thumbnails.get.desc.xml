<webscript>
  <shortname>Get thumbnails</shortname>
  <description>Gets the thumbnails for a content resource.</description>
  <url>/api/node/{store_type}/{store_id}/{id}/content{property}/thumbnails</url>  
  <url>/api/path/{store_type}/{store_id}/{id}/content{property}/thumbnails</url>  
  <format default="json"/>
  <authentication>user</authentication>
  <transaction allow="readonly">required</transaction>
</webscript>