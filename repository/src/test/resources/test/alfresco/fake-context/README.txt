This is a small set of spring context files, including overrides and
 references.

It is used when testing out org.alfresco.util.ContextDependencyLister

This allows us to test new features on a controlled, small configuration
 set, before going live on the full one.
