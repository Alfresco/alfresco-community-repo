<webscript>
  <shortname>Retrieve list of all Types (getTypes)</shortname>
  <description>
<![CDATA[
Returns the list of all types in the repository.<br>
<br>
Inputs:<br>
<br>
(Optional) ID typeId: not set (default)<br>
(Optional) Bool returnPropertyDefinitions: False (default)<br>
(Optional) int maxItems: 0 = Repository-default number of items (Default)<br>
(Optional) int skipCount: 0 = start (Default)<br>
<br>
Outputs:<br>
<br>
Result Set - List of types<br>
Boolean hasMoreItems<br>
The type attributes of each type will be returned<br>
The property definitions of each type will be returned if returnPropertyDefinitions is TRUE.<br>
<br>
Notes:<br>
<br>
A repository may support a hierarchy of types but CMIS will return them as a flat list.<br>
If provided, the input parameter “TypeId” specifies to only return the specific Object Type and its descendants. If not provided, all Object Types are to be returned.<br> 
If no “maxItems” value is provided, then the Repository will determine an appropriate number of items to return. How the Repository determines this value is repository-specific and opaque to CMIS.<br> 
If “returnPropertyDefinitions” is False, then the Repository will return only the “Attributes” of the Object Type Definition as specified in the “Object Type” section of the Data Model. Otherwise, property definitions will also be returned for each object type.
]]>
</description>
  <url>/api/types?type={type?}&amp;includePropertyDefinitions={includePropertyDefinitions?}&amp;skipCount={skipCount?}&amp;maxItems={maxItems?}</url>
  <authentication>user</authentication>
  <format default="atomfeed"/>
  <family>CMIS</family>
</webscript>