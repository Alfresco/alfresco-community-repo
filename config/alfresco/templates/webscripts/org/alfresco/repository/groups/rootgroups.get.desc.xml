<webscript>
   <shortname>List all root groups</shortname>
   <description><![CDATA[
      Lists all root groups. The following optional parameters are available :-
      <ul>
      <li>zone - If set to 'true', returns root groups from the specified zone.   
      If not specified, returns groups from all zones.
      </li>
      <li>
      shortNameFilter - Returns those root groups with a partial match on shortName.    
      The shortname filter can contain the wild card characters * and ? but these must be url encoded for this script.
      </li>
      </ul>
   ]]>
   </description>
   <url>/api/rootgroups?shortNameFilter={shortNameFilter?}&amp;zone={zone?}</url>
   <format default="json">argument</format>
   <authentication>user</authentication>
   <transaction allow="readonly">required</transaction>
   <lifecycle>draft_public_api</lifecycle>
</webscript>